<div class="form-group">
  <label for="{{$name}}">{{$name}}</label>
  <input type="text" name="{{$name}}" id="{{$name}}" class="form-control" placeholder="{{$name}}" aria-describedby="{{$name}}">
  <small id="{{$name}}" class="text-muted">Enter Your {{$name}}</small>
</div>